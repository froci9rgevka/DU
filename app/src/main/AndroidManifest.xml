<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.pea.du">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    ///////////////////////
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>
    ////////////////////////
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.pea.du.fileprovider.READ"/>
    //////////////////////////////////////////////////////
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_domain_black_48dp"
            android:label="@string/app_name"
            android:largeHeap="true"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".actyvities.MainActivity">
        </activity>
        <activity
                android:name=".actyvities.inspect.InspectionActivity"
                android:label="@string/title_activity_inspection"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".actyvities.inspect.InspectionDetailsActivity"
                android:label="@string/title_activity_content_results_child">
        </activity>
        <activity
                android:name=".actyvities.addresses.works.WorksActivity"
                android:label="@string/title_activity_defect">
        </activity>
        <activity android:name=".actyvities.addresses.works.photo.PhotoLibraryItem">
        </activity>
        <activity android:name=".actyvities.addresses.works.photo.PhotoDetails">
        </activity>

        <provider
                android:name="android.support.v4.content.FileProvider"
                android:authorities="com.pea.du.android.fileprovider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".actyvities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".actyvities.addresses.AddressesActivity">
        </activity>

        <service android:name=".web.stubAuthenticator.AuthenticatorService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>

            <meta-data
                    android:name="android.accounts.AccountAuthenticator"
                    android:resource="@xml/authenticator"/>
        </service>
        <service
                android:name=".web.stubAuthenticator.syncAdapter.SyncService"
                android:exported="true"
                android:process=":sync">
            <intent-filter>
                <action android:name="android.content.SyncAdapter"/>
            </intent-filter>

            <meta-data
                    android:name="android.content.SyncAdapter"
                    android:resource="@xml/syncadapter"/>
        </service>

        <provider
                android:name=".web.stubAuthenticator.provider.StubProvider"
                android:authorities="com.pea.du.android.datasync.provider"
                android:exported="false"
                android:syncable="true"/>

        <activity android:name=".actyvities.addresses.works.defectation.AddActActivity">
        </activity>
        <activity android:name=".actyvities.addresses.another.StageWork">
        </activity>
    </application>

</manifest>